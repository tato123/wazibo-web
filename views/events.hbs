<div class="container">
    <ul class="list-unstyled list-inline list-nav">
      <li>
        <a href="/items" >Items for sale</a>
      </li>
       <li>
        <a href="/" class="active">Events</a>
      </li>
    </ul>

    <div style="box-shadow: 0 1px 2px rgba(0,0,0,.1);background-color:#fff;">
    {{#each items}}
        {{> event}}
    {{else}}
        <h4>Aww, it looks like there aren't any events. Be a trend setter and create one!</h4>
    {{/each}}
    </div>
</div>
